<form method="post" action="{{ path('app_sousmission_delete', {'id': sousmission.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sousmission.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
