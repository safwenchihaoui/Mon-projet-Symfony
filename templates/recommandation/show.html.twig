{% extends 'adminbase.html.twig' %}

    {% block stylesheet %}
    {{parent()}}
    {% endblock %}
      {% block sidebar %}
        {{parent()}}
      {% endblock %}

    
        {% block Dashboard %}
          <div class="main-panel">
            <div class="container">
              <div class="page-inner">
                <div class="d-flex align-items-left align-items-md-center flex-column flex-md-row pt-2 pb-4" >
                  <div>
                    <h3 class="fw-bold mb-3">Dashboard</h3>
                
                  </div>
                </div>
                 <table class="table">
                      <tbody>
                          <tr>
                              <th>Id</th>
                              <td>{{ recommandation.id }}</td>
                          </tr>
                          <tr>
                              <th>Titre</th>
                              <td>{{ recommandation.titre }}</td>
                          </tr>
                          <tr>
                              <th>Description</th>
                              <td>{{ recommandation.description }}</td>
                          </tr>
                          <tr>
                              <th>souscategorieRecommand</th>
                              <td>{{ recommandation. SouscategorieRecommand.titre}}</td>
                          </tr>
                          <tr>
                              <th>Image</th>
                           <td> <img src="{{ asset('/uploads/'~ recommandation.image) }}" width="100"   alt=""></td>

                          </tr>
                          <tr>
                              <th>Pdf</th>
                              <td><img src="{{asset('/uploads/'~ recommandation.pdf)}}" target="_blank"  >   </td>
                          </tr>
                          <tr>
                              <th>Lien</th>
                              <td><a href="{{recommandation.lien}}" target="_blank" >{{recommandation.lien}}</a></td>
                          </tr>
                      </tbody>
                  </table>

                  <a href="{{ path('app_recommandation_index') }}">back to list</a>

                  <a href="{{ path('app_recommandation_edit', {'id': recommandation.id}) }}" class="btn btn-info">edit</a>

                  {{ include('recommandation/_delete_form.html.twig') }}
              
              </div>
            </div>

    
  
    
                  {% block footer %}
                    {{parent()}}
                  {% endblock %}
           </div>
      
        {% endblock %}
           
                
            
       {% block javascript %}
         {{parent()}}
       {% endblock %}  

    

